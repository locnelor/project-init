import { SysMenuOnRoleEntity } from './sys.menu.on.role.entity';

describe('SysMenuOnRoleEntity', () => {
  it('should be defined', () => {
    expect(new SysMenuOnRoleEntity()).toBeDefined();
  });
});
